package week5.HomeWork;

public class RepeateChar {

	public String findDuplicateChar(String str) {

		/*
		 * Substrings of Size Three with Distinct Characters A string is good if there
		 * are no repeated characters.
		 * 
		 * Given a string s​​​​​, return the number of good substrings of length three
		 * in s​​​​​​.
		 * 
		 * Note that if there are multiple occurrences of the same substring, every
		 * occurrence should be counted.
		 * 
		 * A substring is a contiguous sequence of characters in a string Input: s =
		 * "xyzzaz" Output: 1 Explanation: There are 4 substrings of size 3: "xyz",
		 * "yzz", "zza", and "zaz". The only good substring of length 3 is "xyz"
		 * 
		 * input is String
		 * Output find duplicate char count
		 * 
		 * String str = "aabcc"
		 * output int i = 2;
		 * iterate the 
		 * Convert the String in char str.toCharAt(str)
		 * a =a
		 * tmep = a
		 * 
		 * 
		 */
		return str;

	}

}
